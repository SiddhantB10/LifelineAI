rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development mode rules: allow app reads/writes while integrating auth/API.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
